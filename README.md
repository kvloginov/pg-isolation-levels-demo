## SQL isolation levels demo

*work in progress*

This repository contains a demo of SQL isolation levels and how they affect the outcome of a transaction.

The demo is implemented in Golang using the `sqlx` library.